<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.KnowledgeDocMapper">

    <insert id="insert" parameterType="com.sky.entity.KnowledgeDoc" useGeneratedKeys="true" keyProperty="id">
        insert into knowledge_doc
        (title, category, content, status, create_time, update_time)
        values
        (#{title}, #{category}, #{content}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.sky.entity.KnowledgeDoc">
        update knowledge_doc
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="category != null and category != ''">
                category = #{category},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="pageQuery" resultType="com.sky.entity.KnowledgeDoc">
        select *
        from knowledge_doc
        <where>
            <if test="title != null and title != ''">
                and title like concat('%', #{title}, '%')
            </if>
            <if test="category != null and category != ''">
                and category = #{category}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by update_time desc
    </select>

</mapper>
