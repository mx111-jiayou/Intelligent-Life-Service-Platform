<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SlowSqlLogMapper">

    <insert id="insert" parameterType="com.sky.entity.SlowSqlLog" useGeneratedKeys="true" keyProperty="id">
        insert into slow_sql_log
        (mapper_id, sql_text, normalized_sql, sql_signature, cost_time, threshold_time, create_time)
        values
        (#{mapperId}, #{sqlText}, #{normalizedSql}, #{sqlSignature}, #{costTime}, #{thresholdTime}, #{createTime})
    </insert>

    <select id="pageQuery" resultType="com.sky.entity.SlowSqlLog">
        select *
        from slow_sql_log
        <where>
            <if test="mapperId != null and mapperId != ''">
                and mapper_id like concat('%', #{mapperId}, '%')
            </if>
            <if test="sqlSignature != null and sqlSignature != ''">
                and sql_signature = #{sqlSignature}
            </if>
            <if test="beginTime != null">
                and create_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and create_time &lt;= #{endTime}
            </if>
        </where>
        order by create_time desc
    </select>

</mapper>
