<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.AiToolCallLogMapper">

    <insert id="insert" parameterType="com.sky.entity.AiToolCallLog" useGeneratedKeys="true" keyProperty="id">
        insert into ai_tool_call_log
        (user_id, session_id, tool_name, tool_params, tool_result, success, create_time)
        values
        (#{userId}, #{sessionId}, #{toolName}, #{toolParams}, #{toolResult}, #{success}, #{createTime})
    </insert>

</mapper>
